# sUAS-KF-ML-Simulation
Hybrid state estimation using a UKF and a neural network measurement model for fault-aware sensor fusion. The state includes sensor health, allowing the filter to detect faults and adaptively downweight corrupted measurements, improving robustness to bias, noise, and sensor dropouts 
